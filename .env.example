RPC_ENDPOINT=https://api.mainnet-beta.solana.com

# Token metadata
TOKEN_NAME=Narrative Token
TOKEN_SYMBOL=NARR
TOKEN_URI=https://example.com/metadata.json

# Launch + buy defaults
DEFAULT_BUY_SOL=1.5
DEFAULT_SLIPPAGE_BPS=300

# Buyback loop controls
BUYBACK_POLL_INTERVAL_MS=10000
BUYBACK_TRIGGER_SOL=0.1
BUYBACK_SOL_CAP=0.5

# Distribution loop
DISTRIBUTION_BATCH_SIZE=20
DISTRIBUTION_SLEEP_MS=30000
MIN_DISTRIBUTABLE_TOKENS=1
MIN_HOLDER_BALANCE_SOL=5
MIN_HOLDER_TOKEN_AMOUNT=0

# Path to JSON file containing holder lists (can be updated at runtime)
HOLDERS_JSON_PATH=./holders.json

# Dev wallet secret: base58 string or JSON array of 64 bytes.
DEV_WALLET_SECRET=base58-or-json-secret-here
